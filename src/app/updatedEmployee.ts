/* Updated Employee interface represents an employee that is updating their info with a eId, fName, lName, phone number, email */
export interface UpdatedEmployee {
    eId: number;
    fName: string;
    lName: string;
    email: string;
    phone: string;
  }
